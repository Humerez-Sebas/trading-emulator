# SDD Progress Ledger — RFC-012 Performance

- **Plan:** `docs/superpowers/plans/2026-07-04-rfc-012-performance.md`
- **RFC:** `docs/architecture/rfcs/012-performance.md`
- **Branch:** `feature/rfc-012-performance` (off develop @ cce9e80 — RFC-011 PR #30 merged)
- **Base commit:** `cce9e80`

## Task Progress

Task 1 (shared-candle-series reference-identity proof, R4): complete (commit 4ef3930, verified 2026-07-04: 928 tests green at commit time, tsc app+spec clean, lint 0; Opus review PASS, 0 findings of any severity)
  - Proof validity independently confirmed non-tautological: panelChartView$ candles = direct `series[descriptor.timeframe]` property read, zero copy sites in the mapper (the only .map is memoizeMap on the trade-overlay path); a future .slice()/spread WOULD fail the toBe gate. Cross-instance independence real (mappers new'd via runInInjectionContext, own memo slots; shared ref comes from the single store slice).
  - NOTES for Tasks 2-5 (carry into briefs): (1) HARNESS: bare `npx vitest run <spec>` fails with "TestBed.initTestEnvironment" for ALL these specs — only `npm test`/`ng test` bootstraps the env; never judge a spec red from bare vitest. (2) The sync-read pattern (subscribe then immediate expect) is valid ONLY because configurePanel runs BEFORE subscribe (ReplaySubject buffer) + MockStore emits synchronously — keep that order. (3) Test-2 nuance: same-value overrideSelector + refreshState memo-hits and distinctUntilChanged suppresses — to exercise a REAL post-tick re-emission, change a value (bump currentTime/utcOffset). (4) `?? []` fallback: a panel on an UNLOADED timeframe gets a fresh [] per memo-miss (NOT shared) — Task 5's 8-panel scenario must seed every profiled TF or empty-series panels won't share references.
Task 2 (D6 update-gating proof: hidden panel zero render work, one resync on show): complete (commit 1c60b3c, verified 2026-07-04: 931 tests green at commit (auditor re-ran in isolated worktree @ 1c60b3c while Task 3's mid-TDD tree was transiently broken), tsc app+spec clean, lint 0; Opus review PASS, 0 findings)
  - Surrogate validated 1:1: chart.component.ts:538-578 — all five engine side-effects (applyColors/render/pushSession/pushDrawings/pushTrading) are driven exclusively by the five gated mapper streams; the only bare store.select there (selectActiveTfShortfall) sets a banner signal, no render. Counting emissions IS counting engine work.
  - gated() traced (mapper:109-116, combineLatest+filter+map+distinctUntilChanged): zero-while-disabled = filter; exactly-one-LATEST catch-up = combineLatest holds only latest (no per-tick replay); no-op re-enable = reference-based distinctUntilChanged on a stable mapped object. 2-of-5 streams ruled REPRESENTATIVE (all five share the same gated() operator; the two tested cover both stream shapes: mapped + direct-selector).
  - Step 3 conditional getter correctly NOT fired; mapper service untouched. Task 5 may reuse the emission-counting surrogate as validated.
Task 3 (lazy chart creation on first show — sticky @if latch in ChartPanelComponent): complete (commit 499a48a, verified 2026-07-04: 934 tests green at commit, tsc app+spec clean, lint 0; genuine red phase (2 failed | 932 passed pre-implementation); Opus review PASS, 0 findings)
  - Latch semantics verified: signal(false) set only by the visible() effect, no reset path; viewport `track pid` preserves instances across tab switches so re-creation cannot clear it; @if gates ONLY <app-chart>, header unconditional. RFC-009 keep-alive composition intact (mounted engine + gated mapper when hidden-after-show); engine construction confirmed in chart.component ngAfterViewInit → unmounted = fully deferred; registry handle still registered eagerly in ngOnInit with null-tolerant delegates (behaviorally identical to pre-RFC-012 not-yet-ready panels).
  - Deviation ratified: `private hasBeenVisible` fails strictTemplates (tsconfig.json:22) for template-bound members → declared readonly non-private, matching the file's other template-bound members. Known sanctioned deviation, not a defect.
  - NOTE for Task 4/final audit: registry liveness is deliberately DECOUPLED from mounting — instance-count proofs must count leaf <app-chart> mounts (stub), never registry.count().
Task 4 (lazy-creation instance-count proof at the viewport): complete (commit e480da8, verified 2026-07-04: 936 tests green at commit, tsc app+spec clean, lint 0; Opus review PASS, 0 findings)
  - Real-chain proof verified link-by-link with NO shortcuts: reducer-shaped LayoutState (invariant-honest fixture) -> real selectVisiblePanelIds (NOT overridden) -> viewport [visible] binding -> real ChartPanelComponent sticky latch -> @if gate; only the leaf app-chart swapped for a counting stub. Initial mount 2 (active tab only), 4 after activating tab-b (sticky keep-alive). Counts leaf mounts via By.directive, never registry.count() (decoupled by design).
  - resetSelectors afterEach correctly ABSENT (viewport/panel spec families omit it; only mapper specs use it — convention verified against both sources).
  - PROCESS NOTE: a git-index race briefly swept this spec into the Task 3 ledger commit; the implementer repaired history (11f7aa7 ledger-only + e480da8 spec-only, verified clean). Rule going forward: orchestrator ledger commits during parallel implementation use explicit pathspec commits.
Task 5 (8-panel deterministic profiling suite + documented findings): complete (commit df49278, verified 2026-07-04: 939 tests green, tsc app+spec clean, lint 0; individual Opus audit FOLDED INTO the final whole-branch audit — proof-only task, tests incidentally verified green in two prior parallel audit runs, scope verified 2 files by orchestrator)
  - MEASURED (recorded in the plan's findings section, edits confined to the bracketed markers): all 8 same-symbol mappers share ONE Candle[] (=== on a 300-bar M1, O(1) memory); one clock tick with 8 visible = exactly 8 panelChartView$ recomputes (linear, not 64); gating 3 of 8 caps chartView$ emissions at 5 (hidden panels contribute 0). Conclusion recorded: no Web Worker (D7), no second cache (R4) warranted.
  - Third test counts the real gated chartView$ (selectChartView override with the real projection shape), not the chartStyle$ fallback.
Final audit: incomplete
