# SDD Progress Ledger — RFC-011 Workspace Layout Persistence

- **Plan:** `docs/superpowers/plans/2026-07-03-rfc-011-layout-persistence.md`
- **RFC:** `docs/architecture/rfcs/011-workspace-layout-persistence.md`
- **Branch:** `feature/rfc-011-layout-persistence` (off develop @ 8e89142 — RFC-010 PR #29 merged)
- **Base commit:** `8e89142`

## Task Progress

Task 1 (SessionPayloadV2 model + pure migrateV1ToV2/parseSessionPayload): complete (commits df7a49b + fix 3c323c3 + audit fix 60ac114, verified 2026-07-03: 901 tests green, tsc app+spec clean, lint 0; Opus review FAIL->fixed->closed, 0 High/Critical remaining)
  - Step 0 outcome: NO rename of layout-invariants.spec-util.ts — but orchestrator build probe caught a High the plan's tsc sentinel missed: production import of the spec-util pulls `vitest` into the app bundle (probe: "expect-type is not ESM" warning + magic-string-es/index chunks). Fix 3c323c3: new production-safe `layout-invariants.ts` (pure `layoutInvariantViolation`/`isLayoutConsistentPure`, zero vitest); spec-util keeps `assertLayoutConsistent` signature, delegates via expect(violation).toBeNull(); session-migration imports only the pure checker. Probe re-run clean.
  - Opus audit HIGH (closed by 60ac114): 3c323c3 dropped df7a49b's try/catch, so a V2-labeled payload with structurally malformed layout (null/{}/tabs:null/non-array cells/panelIds/null panels — typeof null === 'object' passes isSessionPayloadV2) made parseSessionPayload THROW instead of falling back. Fix: shape guard at top of layoutInvariantViolation returns 'malformed layout state'; regression spec iterates all 6 malformed shapes + asserts non-layout fields survive fallback. Fix applied inline by orchestrator per auditor's own empirically pre-verified prescription (re-audit dispatch skipped as redundant; auditor had already validated the exact guard against all repro shapes).
  - Rule for Tasks 2-5: any production consumer of layout invariants imports `layout-invariants.ts`, NEVER `layout-invariants.spec-util.ts` (vitest-carrying).
  - Lint deviation ratified: destructure-drop `_drop` pattern violates no-unused-vars (no ignore override in eslint.config.js) — spec uses `delete rest['schemaVersion']` instead.
Task 2 (restore actions: restoreLayout / restoreGroups / restoreDrawingsForSymbol): complete (commit 9f730d9, verified 2026-07-03: 907 tests green, tsc app+spec clean, lint 0; Opus review PASS, 0 High/Critical)
  - Additive-only confirmed (the single -1 was the layout.actions.ts import reshape; restoreDrawings array form byte-unchanged). Reducers trust input wholesale BY DESIGN — validation lives in parseSessionPayload; the three actions have zero production dispatchers until Task 5.
  - Low (docs only): task-2-report prose misattributed the -1 deletion to the drawings spec.
  - HANDOFF to Task 5: (1) thenRestore is the first raw-storage->restoreLayout path — parseSessionPayload MUST gate it; (2) restoreDrawingsForSymbol silently restores [] for an absent/wrong symbol — primarySymbol correctness at dispatch is load-bearing; (3) PanelDescriptor.symbol allows '' (active-asset sentinel) — restored layouts carrying '' are valid and downstream must resolve it.
Task 3 (workspace meta snapshot + Supabase toPayload/fromPayload V2 mapping): complete (commit 979eca6, verified 2026-07-03: 912 tests green, tsc app+spec clean, lint 0, prod build clean/vitest-free at 608.18 kB; Opus review PASS, 0 High/Critical)
  - toPayload stamps V2 (single planned D9 change; row-level test untouched); fromPayload(p, primarySymbol) REQUIRED param delegates to parseSessionPayload; snapshot selector = ONE static createSelector, 11 inputs (D8 held). Third fromPayload call site (sesiones-page card.symbol) verified as genuine primary symbol.
  - Interim stopgap (BY DESIGN, Task 4 replaces): buildFlattenInput synthesizes singlePanelLayoutFor(meta.symbol, meta.activeTf ?? 'M1') + linkGroups [] because WorkspaceMeta lacks the fields; real meta.drawings preserved; snapshot already carries real layout/panels/linkGroups.
  - MEDIUMS/LOWS folded into Task 4 brief: (M1) CloudSessionRow.schemaVersion/schema_version column stays 1 while payload is 2 — write-only metadata today (mergeByLww ignores it, display-only reader), reconcile in Task 4/5; (L1) persistMeta$ spreads the widened snapshot into putMeta, silently writing layout/panels/RECORD-shaped linkGroups into IndexedDB before WorkspaceMeta declares them — Task 4 must type the fields AND introduce the Record->LinkGroup[] conversion at the flatten site (the single biggest open correctness item); (L2) fetchPayload return type still bare SessionPayloadV1 — widen to the union/StoredSessionPayload.
Task 4 (workspace-db IndexedDB threading of layout/panels/linkGroups, no DB_VERSION bump): complete (commit bb923ab, verified 2026-07-04: 916 tests green, tsc app+spec clean, lint 0; Opus review PASS, 0 High/Critical/Medium)
  - Persist side CONFIRMED COMPLETE end-to-end: snapshot (Record linkGroups) -> persistMeta$/doSwitch Object.values conversion (tsc flagged BOTH putMeta sites; stale MetaSnapshot alias corrected, local/non-exported) -> WorkspaceMeta (array) -> buildFlattenInput meta-first reads -> V2 payload; flushOnEdit$ discards the snapshot value so the Record never leaks to storage/wire.
  - Folded Task-3 findings closed: buildRow stamps payload.schemaVersion (rows now write schema_version 2; nothing branches on it); fetchPayload widened to V1|V2 (type-only ripple into sesiones-page PendingDownload/materializeAndOpen; card.symbol intact).
  - Lows (logged, no fix): (1) independent `?? fallback` pairing in buildFlattenInput could mix real layout with fallback panels IF a future writer ever stores one without the other (today unreachable: all writers emit both together); (2) push-test layout/panels half coincides with the fallback when activeTf='M1' — saved from tautology by the non-empty linkGroups assertion.
  - HANDOFF to Task 5: PendingSessionRestore (workspaces.effects.ts ~226-240) must gain layout/panels/linkGroups + populate sites; read side already produces them via fromPayload/reconstructWorkspaces; doSwitch thenRestore adds the three dispatches; any reader of CloudSessionRow.schemaVersion should expect 2.
Task 5 (restore dispatch wiring + .session.json thenRestore + persist→restore e2e): complete (commit 52ff4d2, verified 2026-07-04: 923 tests green, tsc app+spec clean, lint 0, build clean 608.82 kB vitest-free; Opus review PASS, 0 High/Critical)
  - Primary restore path (plan D: reducer-level workspaceRestored on loadInitial+doSwitch) fully wired: layout/link-groups handlers mirror drawings precedent; fallback singlePanelLayoutFor(workspace.symbol, activeTf ?? 'M1') fires only when layout/panels absent; thenRestore's restoreLayout lands AFTER workspaceRestored so imported values win. e2e proof spec: multi-panel '2h' layout through toPayload->JSON->parseSessionPayload->fromPayload->restore actions->assertLayoutConsistent, discriminating.
  - Ambiguity resolved (audit-verified): restoreDrawingsForSymbol + thenRestore layout push are production-DORMANT by design (SessionFileV1/RestorePlan structurally V1; no populate site exists yet); proven by e2e + reducer specs.
  - Deviation ratified: plan's `return fallback;` was a tsc error — reshaped to { workspace: fallback.layout, panels: fallback.panels }. Low: link-groups "defaults to empty" spec non-discriminating (initial state already {}), disclosed.
  - MEDIUM ESCALATED -> GAP FIX (pre-final-audit): materializeAndOpen (sesiones-page.component.ts ~817-832), the ONLY live production cloud-pull path, discards restored.layout/panels/linkGroups (and per-symbol drawings) when materializing — cross-device cloud restore of a multi-panel layout silently falls back to single-panel default, violating the RFC's "survives a device change" headline. Pre-existing (no RFC-011 task file list covers the site; reconstructWorkspaces, which DOES carry the fields, is spec-only dead code in production). GAP FIX LANDED (commit 75e6a1f, 925 tests green +2 new specs, tsc/lint clean): materializeAndOpen now writes meta.layout/panels/linkGroups from restored.* and meta.drawings from restored.drawings[card.symbol]?.items ?? local ?? []; V2 multi-panel + V1 migration-default cases spec-covered; cross-device cloud restore of multi-panel layouts now works end-to-end. Final audit reviews the fix as part of develop..HEAD.
Final audit: complete (2026-07-04, Opus whole-branch review develop..a552b36: PASS, 0 findings of ANY severity — "Ship it". Gates re-run by auditor: tsc app+spec clean, 925/925 tests, lint 0, build OK 608.82 kB vitest-free. RFC-011 Estado Esperado ✓ item-by-item, incl. explicit device-change confirmation: cross-device cloud restore of multi-panel layouts works end-to-end via the hardened materializeAndOpen (which BOTH cloud-open routes — direct + download-then-open — share). Gap fix 75e6a1f reviewed line-by-line as part of the final audit (it had no individual task audit). One LWW resolution point confirmed (mergeByLww untouched, no second effect/timestamp). Corrupt-row no-throw chain hand-traced. Invariant greps clean: zero audited chart-file touches, zero factory selectors, zero new deps, zero vitest reachable from production. Carried Lows/Mediums all ruled NON-BLOCKING: independent ??-fallback pairing (unreachable divergence), non-discriminating link-groups default spec, production-dormant restoreDrawingsForSymbol/thenRestore-layout (dormant-by-design), MetaSnapshot Record coupling (tsc-enforced), schema_version=2 (nothing branches).)
